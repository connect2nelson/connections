= render 'consultant_skills', consultant: consultant
.small-8.columns
  %ul.matches
    - if mentees.empty?
      %h4 "Couldn't find any matches"
    -else
      - mentees.take(5).each do |connection|
        = render 'connection', connection: connection, sponsorship: false

